name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.AUTOMATION_TOKEN }}
        issue-message: 'Welcome to GPkit! Thank you for bringing this to our attention.'
